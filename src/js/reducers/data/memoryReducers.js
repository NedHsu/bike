import {
    
} from '../../constants/actionTypes';

const defaultState = {
    prop,
};

const memoryReducers = (state = defaultState, action = {}) => {
    switch (action.type) {
        case type:
            return {
                ...state,
            };

        default:
            return state;
    }
};

export default memoryReducers;
