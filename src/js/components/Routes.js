import React, { Component, PropTypes } from 'react';
import Box from 'grommet/components/Box';
import GMap from './GMap';

const Routes = ({
  
}) => (
	<Box
    full={true}>
		<GMap style='width: 100px; height: 100px;'/>
	</Box>
);

export default Routes;